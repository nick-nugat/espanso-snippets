matches:
  # - trigger: ";time"
  #   replace: "{{time}}"
  #   vars:
  #     - name: time
  #       type: date
  #       params:
  #         format: "%H:%M"

  - triggers:
    - ";today"
    - ";date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
      